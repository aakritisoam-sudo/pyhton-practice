s1 = {1,23,45}
s2 = {1,2,23,4,5}

union = s1.union(s2) # it will return a new set with all elements from the both elements
print(union)

intersection = s1.intersection(s2) # it will return a new set with common elements from both sets 
print(intersection)
